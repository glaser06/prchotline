<div class="fields" style="display:none;">
  <div class="large-1 columns">
    <%= l.collection_select(:item_id, Item.all.alphabetical, :id, :name, { :prompt => 'Select Item' } ) %>
    <%= l.input :active %>
    <%= l.link_to_remove("x") %>
  </div>
</div>
