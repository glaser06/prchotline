<div class="fields">
<%= l.input :address %>
<%= l.input :city %>
<%= l.input :zipcode %>
<%= l.collection_select(:county_id, County.all, :id, :name, :prompt => "Choose a county...") %>
<%= l.link_to_remove("x") %>
</div>
