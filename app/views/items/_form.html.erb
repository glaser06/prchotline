<%= simple_form_for @item do |l| %>

<% if item.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

    <ul>
    <% item.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <%= l.input :name %>
  <%= l.input :description %>
  <%= l.input :active %>
  <%= l.button :submit %>
<% end %>
