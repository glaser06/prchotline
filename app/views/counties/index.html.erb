<p id="notice"><%= notice %></p>

<h1>Counties</h1>

<div class="row">
<div class="large-12 columns">
<div class="panel">

<table border= "1px solid black">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% @counties.each do |county| %>
      <tr>
        <td><%= county.name%></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div></div>
</div>

<br>
